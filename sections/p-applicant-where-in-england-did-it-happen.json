{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "http://localhost:3000/schema/definitions/p-applicant-where-in-england-did-it-happen",
  "type": "object",
  "required": ["q-applicant-where-in-england-did-it-happen"],
  "additionalProperties": false,
  "properties": {
    "q-applicant-where-in-england-did-it-happen": {
      "title": "Where in England did the crime happen?",
      "type": "string",
      "uniqueItems": true,
      "items": {
        "oneOf": [
          {
            "title": "London",
            "enum": ["london"]
          },
          {
            "title": "Manchester",
            "enum": ["manchester"]
          },
          {
            "title": "Newcastle",
            "enum": ["newcastle"]
          },
          {
            "title": "Somewhere else",
            "enum": ["somewhere-else"]
          }
        ]
      },
      "errorMessages": {
        "required": "please select an option"
      }
    }
  }
}