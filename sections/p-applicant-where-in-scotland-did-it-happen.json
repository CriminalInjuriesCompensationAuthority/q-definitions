{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "http://localhost:3000/schema/definitions/p-applicant-where-in-england-did-it-happen",
  "type": "object",
  "required": ["q-applicant-where-in-scotland-did-it-happen"],
  "additionalProperties": false,
  "properties": {
    "q-applicant-where-in-scotland-did-it-happen": {
      "title": "Where in Scotland did the crime happen?",
      "type": "string",
      "uniqueItems": true,
      "items": {
        "oneOf": [
          {
            "title": "Glasgow",
            "enum": ["glasgow"]
          },
          {
            "title": "Edinburgh",
            "enum": ["edinburgh"]
          },
          {
            "title": "Aberdeen",
            "enum": ["aberdeen"]
          },
          {
            "title": "Somewhere else",
            "enum": ["somewhere-else"]
          }
        ]
      },
      "errorMessages": {
        "required": "please select an option"
      }
    }
  }
}